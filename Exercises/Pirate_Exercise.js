let cargoHold = ['gold', 'not gold'];
let lootedCargo = [];

let deckMops = function(arr){
    if (arr === 'gold'){
        arr = 'not gold';
    }
    return arr;
}

//Goal to get the gold out of cargohold
